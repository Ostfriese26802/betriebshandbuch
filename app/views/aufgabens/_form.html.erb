<%= form_with(model: aufgaben, local: true) do |form| %>
  <% if aufgaben.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aufgaben.errors.count, "error") %> prohibited this aufgaben from being saved:</h2>

      <ul>
        <% aufgaben.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :zeitpunkt %>
    <%= form.text_field :zeitpunkt %>
  </div>

  <div class="field">
    <%= form.label :beschreibung %>
    <%= form.text_area :beschreibung %>
  </div>

  <div class="field">
    <%= form.label :applikation_id %>
    <%= form.text_field :applikation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
