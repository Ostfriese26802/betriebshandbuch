<%= form_with(model: kunde, local: true) do |form| %>
  <% if kunde.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kunde.errors.count, "error") %> prohibited this kunde from being saved:</h2>

      <ul>
        <% kunde.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :kuerzel %>
    <%= form.text_field :kuerzel %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
