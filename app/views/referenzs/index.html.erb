<p id="notice"><%= notice %></p>

<h1>Referenzs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Link</th>
      <th>Applikation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @referenzs.each do |referenz| %>
      <tr>
        <td><%= referenz.name %></td>
        <td><%= referenz.link %></td>
        <td><%= referenz.applikation_id %></td>
        <td><%= link_to 'Show', referenz %></td>
        <td><%= link_to 'Edit', edit_referenz_path(referenz) %></td>
        <td><%= link_to 'Destroy', referenz, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Referenz', new_referenz_path %>
