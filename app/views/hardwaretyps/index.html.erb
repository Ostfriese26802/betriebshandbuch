<p id="notice"><%= notice %></p>

<h1>Hardwaretyps</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hardwaretyps.each do |hardwaretyp| %>
      <tr>
        <td><%= hardwaretyp.name %></td>
        <td><%= link_to 'Show', hardwaretyp %></td>
        <td><%= link_to 'Edit', edit_hardwaretyp_path(hardwaretyp) %></td>
        <td><%= link_to 'Destroy', hardwaretyp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hardwaretyp', new_hardwaretyp_path %>
