<%= form_with(model: hardwaretyp, local: true) do |form| %>
  <% if hardwaretyp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hardwaretyp.errors.count, "error") %> prohibited this hardwaretyp from being saved:</h2>

      <ul>
        <% hardwaretyp.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
