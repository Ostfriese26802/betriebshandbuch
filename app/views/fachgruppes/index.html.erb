<p id="notice"><%= notice %></p>

<h1>Fachgruppen</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Telefonnummer</th>
      <th>Bereitschaft</th>
      <th>Email</th>
      <th>Ticketgruppe</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fachgruppes.each do |fachgruppe| %>
      <tr>
        <td><%= fachgruppe.name %></td>
        <td><%= fachgruppe.telefonnummer %></td>
        <td><%= fachgruppe.bereitschaft %></td>
        <td><%= fachgruppe.email %></td>
        <td><%= fachgruppe.ticketgruppe %></td>
        <td><%= link_to 'Anzeigen', fachgruppe %></td>
        <td><%= link_to 'Bearbeiten', edit_fachgruppe_path(fachgruppe) %></td>
        <td><%= link_to 'Löschen', fachgruppe, method: :delete, data: { confirm: 'Sind Sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Fachgruppe', new_fachgruppe_path %>
