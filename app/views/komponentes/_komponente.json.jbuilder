json.extract! komponente, :id, :name, :beschreibung, :installationspfad, :port, :start, :stop, :besonderheiten, :applikation_id, :created_at, :updated_at
json.url komponente_url(komponente, format: :json)
