<p id="notice"><%= notice %></p>

<h1>Betriebssystemtyps</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @betriebssystemtyps.each do |betriebssystemtyp| %>
      <tr>
        <td><%= betriebssystemtyp.name %></td>
        <td><%= link_to 'Show', betriebssystemtyp %></td>
        <td><%= link_to 'Edit', edit_betriebssystemtyp_path(betriebssystemtyp) %></td>
        <td><%= link_to 'Destroy', betriebssystemtyp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Betriebssystemtyp', new_betriebssystemtyp_path %>
