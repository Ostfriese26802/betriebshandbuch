<p id="notice"><%= notice %></p>

<h1>Stuves</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Beschreibung</th>
      <th>Kunde</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stuves.where(kunde_id: params[:kunde_id]).each do |stufe| %>
      <tr>
        <td><%= stufe.name %></td>
        <td><%= stufe.beschreibung %></td>
        <td><%= Kunde.find(stufe.kunde_id).name %></td>
        <td><%= link_to 'Anzeigen', stufe %></td>
        <td><%= link_to 'Bearbeiten', edit_stufe_path(stufe) %></td>
        <td><%= link_to 'Löschen', stufe, method: :delete, data: { confirm: 'Sind Sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Stufe', new_stufe_path %>
