<%= form_with(model: techbenutzer, local: true) do |form| %>
  <% if techbenutzer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(techbenutzer.errors.count, "error") %> prohibited this techbenutzer from being saved:</h2>

      <ul>
        <% techbenutzer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :kennwort %>
    <%= form.text_field :kennwort %>
  </div>

  <div class="field">
    <%= form.label :applikation_id %>
    <%= form.collection_select(:applikation_id,Applikation.all,:id,:name, :selected => @techbenutzer.applikation_id)%> 
  </div>

  <div class="field">
    <%= form.label :beschreibung %>
    <%= form.text_area :beschreibung %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
